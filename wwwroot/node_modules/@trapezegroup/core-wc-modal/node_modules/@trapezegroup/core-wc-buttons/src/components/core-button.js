import { CoreBaseButton } from './core-base-button.js';

/** This class creates a custom button by extending the CoreButton */
class CoreButton extends CoreBaseButton {
    /** @inheritdoc */
    constructor() {
        // Must call superconstructor first.
        super();
    }
}
customElements.define('core-button', CoreButton);
