// @ts-check
import { TcfTimeSpan } from '/node_modules/@trapezegroup/core-js-tcftypes/src/lib/types.js';
import { CoreInputBase } from './core-input-base.js';

/** This is Custom lit Element for the time field */
class CoreInputTimeSpan extends CoreInputBase {
    /** @inheritdoc */
    constructor() {
        super();
    }

    /** */
    get type() {
        return TcfTimeSpan;
    }
    /**  */
    set type(type) {
        this.type = type;
    }
}

// Note the tag name, class name and file name are all similar.
customElements.define('core-input-timespan', CoreInputTimeSpan);
