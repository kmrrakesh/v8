/* eslint-disable require-jsdoc */
// @ts-check
import { TcfBoolean } from '/node_modules/@trapezegroup/core-js-tcftypes/src/lib/types.js';
import { CoreInputToggleBase } from './core-input-toggle-base.js';

export class CoreInputToogleSelectors extends CoreInputToggleBase {
    static DEFAULT_DATATYPE = TcfBoolean;
    setData(data) {
        this.statuses = [];
        for (let i = 0, len = data.length; i < len; i++) this.statuses.push(data[i].value);
    }
}

customElements.define('core-input-toggle-selectors', CoreInputToogleSelectors);
