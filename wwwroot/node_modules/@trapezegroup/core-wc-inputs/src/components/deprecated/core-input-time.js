// @ts-check
import { TcfTime } from '/node_modules/@trapezegroup/core-js-tcftypes/src/lib/types.js';
import { CoreInputBase } from './core-input-base.js';

/** This is Custom lit Element for the time field */
class CoreInputTime extends CoreInputBase {
    /** @inheritdoc */
    constructor() {
        super();
    }

    /**  */
    get type() {
        return TcfTime;
    }

    /**  */
    set type(type) {
        this.type = type;
    }
}

// Note the tag name, class name and file name are all similar.
customElements.define('core-input-time', CoreInputTime);
