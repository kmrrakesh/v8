import { CoreDatetimePickerBase } from './core-datetime-picker-base.js';

/** @typedef {import('lit-element').TemplateResult} TemplateResult */

/** This is an example of a component which extends the litElement */
class CoreTime extends CoreDatetimePickerBase {
    /** We can initialize the properties here in the constructor */
    constructor() {
        // Must call superconstructor first.
        super();
    }

    /** @inheritdoc */
    get calendarOptions() {
        return { type: 'time' };
    }
}
customElements.define('core-time', CoreTime);
