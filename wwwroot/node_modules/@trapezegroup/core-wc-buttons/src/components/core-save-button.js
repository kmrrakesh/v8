import { CoreBaseButton } from './core-base-button.js';

/** This class creates a yes button by extending the CoreCustomButton */
class CoreSaveButton extends CoreBaseButton {
    /** @inheritdoc */
    constructor() {
        // Must call superconstructor first.
        super();

        /** @type {import('./core-base-button.js').ButtonType} */
        this.buttonType = 'positive';
        this.buttonTitle = 'core-wc-buttons:save';
    }

    /** @inheritdoc */
    get buttonEventName() {
        return 'saveAction';
    }
}
customElements.define('core-save-button', CoreSaveButton);
