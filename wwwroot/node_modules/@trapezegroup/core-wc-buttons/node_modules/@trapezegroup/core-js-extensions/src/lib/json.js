// @ts-check

/**
 * Warning, do not pass objects containing circular references.
 * Will perform a deep clone on an object.
 * @param {object} obj - object to clone
 */
export function clone(obj) {
    return JSON.parse(JSON.stringify(obj));
}
